#!/usr/bin/env bash
# Auto-set PYTHONPATH
export PYTHONPATH="$(cd "$(dirname "$0")/../src" && pwd):$PYTHONPATH"
# Execute main_wrapper.py with venv Python
exec "$(cd "$(dirname "$0")/.." && pwd)/venv/bin/python3" "$(cd "$(dirname "$0")/../src" && pwd)/dr_core_lite/main_wrapper.py" "$@"
